{% set _route = app.request.attributes.get('_route') %}
{% set _route_params = app.request.get('_route_params') %}

<ul class="nav">

	{# <li{% if '#' == _route %} class="active"{% endif %}>
		<a href="{{ path('route') }}">{{ 'item'|trans }}</a>
	</li> #}

	{# <li class="dropdown">
		<a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ 'slidedown'|trans }} <b class="caret"></b></a>
		<ul class="dropdown-menu" role="menu">
			<li>
				<a href="#">{{ 'item 1'|trans }}</a>
			</li>
			<li>
				<a href="#">{{ 'item 1'|trans }}</a>
			</li>
		</ul>
	</li> #}

</ul>